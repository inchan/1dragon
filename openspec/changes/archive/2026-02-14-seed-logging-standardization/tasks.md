## 1. Logging import and utilities

- [x] 1.1 각 시드 파일에 `logger` import 추가

## 2. Per-record logs

- [x] 2.1 `model-persona-presets.seed.ts`의 업데이트/삽입 로그를 `logger.info`로 변경
- [x] 2.2 `style-presets.seed.ts`의 업데이트/삽입 로그를 `logger.info`로 변경
- [x] 2.3 `plans.seed.ts`의 업데이트/삽입 로그를 `logger.info`로 변경
- [x] 2.4 `platform-specs.seed.ts`의 업데이트/삽입 로그를 `logger.info`로 변경

## 3. Completion logs

- [x] 3.1 각 시드 파일의 완료 로그를 `logger.info`로 변경
